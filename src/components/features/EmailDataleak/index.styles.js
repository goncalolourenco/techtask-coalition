import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
  title: {
    paddingTop: '16px',
    paddingBottom: '16px',
  },
  searchButton: {
    marginLeft: '8px',
  },
}));
